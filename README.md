# RAG-Groq Bot

An open-source, RAG-enabled chatbot using Groq for LLM inference and HuggingFace (local or Inference API) for embeddings.

## Setup

1. **Clone & venv**  
   ```bash
   git clone https://github.com/you/rag-groq-bot.git
   cd rag-groq-bot
   python3 -m venv env && source env/bin/activate
